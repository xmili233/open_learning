interface:
  display_name: "Open Learning"
  short_description: "Teach with a live shared paper through the Open Learning CLI"
  default_prompt: "Use $open-learning to guide me through this topic with the shared paper."

policy:
  allow_implicit_invocation: true
